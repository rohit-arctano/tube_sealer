/// Consistent spacing and sizing for a touchscreen UI.
class AppSizes {
  AppSizes._();

  // ── Padding / margin ──
  static const double xs = 4;
  static const double sm = 8;
  static const double md = 16;
  static const double lg = 24;
  static const double xl = 32;

  // ── Touch targets (minimum 48px per Material guidelines) ──
  static const double touchTarget = 56;
  static const double bigButton = 72;

  // ── Card ──
  static const double cardRadius = 12;
  static const double cardPadding = 16;

  // ── Status bar ──
  static const double statusBarHeight = 56;

  // ── Bottom nav ──
  static const double bottomNavHeight = 72;
}
